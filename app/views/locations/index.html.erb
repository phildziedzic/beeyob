<h1>Beeyob Locations:</h1>

<%= form_tag locations_path, method: "GET" do %>
	<%= text_field_tag :q, params[:q], :placeholder => "Enter location name, city, or zip code..." %>
	<%= submit_tag "Search Locations" %>
<% end %>

<%= render "list" %>
